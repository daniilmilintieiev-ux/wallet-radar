node_modules/
dist/
*.log
.env
.env.*
*.sqlite
*.sqlite3
*.db
*.db-*
radar.env*
dist.bak*
.DS_Store

# scratch / debug scripts
_*.mjs

# local SBF build artifacts (built on OrangePi, copied to repo root)
_*.so

# Rust (programs/radar-transfer-hook)
target/

# Serena semantic index (local tool state)
.serena/

# Audit findings doc lives outside the repo (E:/JOB/earn/AUDIT-FINDINGS.md) — never commit
AUDIT-FINDINGS.md

# Audit findings doc (lives outside the repo, never commit)
AUDIT-FINDINGS.md
